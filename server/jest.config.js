// Jest configuration

const config = {
  verbose: true,
  openHandlesTimeout: 100000,
  forceExit: true,
  detectOpenHandles: true,
  testEnvironment: "node",
  testTimeout: 30000,
};

export default config;
